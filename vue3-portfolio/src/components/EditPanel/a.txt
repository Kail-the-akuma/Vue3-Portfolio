<!--  <template>

<header>

   <div className='container header_container'>

     <h5>Hi there welcome to the login Panel!</h5>

     <form class="login_form">

      <p><input :model="email" type="text" placeholder="What is my Username?" /></p>

      <p><input :model="password" type="password"  placeholder="What is my Password?"/></p>

      <p v-if="errorMsg">{{errorMsg}}</p>

      <button @click='register'> Submit</button>

     </form>

   </div>

 </header>


</template>


<script setup>
import{ref} from 'vue'
import{getAuth, signInWithEmailAndPassword} from 'firebase/auth'

const email=ref('')
const password=ref('')
const errorMsg = ref()


const register = () =>{

  const auth = getAuth()

  signInWithEmailAndPassword(auth, email.value, password.value)
    .then((data) => {
      console.log("Succesfull log in")
      console.log(auth.currentUser)
    })

    .catch((error)=>{
      switch(error.code){
        case "auth/invalid-email":
          errorMsg.value = "Ups!! Seems like you don't know the Username!"
          break;
          case "auth/user-not-found":
          errorMsg.value = "Who are you looking for?"
          break;
          case "auth/wrong-password":
          errorMsg.value = "Nice try but no"
          break;
      }
    })
}



</script>

-->